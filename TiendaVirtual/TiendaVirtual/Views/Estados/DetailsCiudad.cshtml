@model TiendaVirtual.Data.Entities.Ciudad
@{
    ViewData["Title"] = "DetailsCiudad";
}

<h1>Detalles</h1>

<div>
    <h4>Ciudad</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="EditCiudad" asp-route-id="@Model?.Id" class="btn btn-outline-warning">Editar</a>
    <a asp-action="Details" asp-route-id="@Model.Estado.Id" class="btn btn-outline-success">Regresar</a>
</div>

